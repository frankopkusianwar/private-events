    <%= form_with url: '/events/:event_id' , local: true, method: :POST do |f| %>
      <%= f.hidden_field :user, value: current_user.id %>
      <%= f.hidden_field :event, value: @event.id %>
      <button type="submit" class="btn btn-primary">Accept invitation</button>
    <% end %>
